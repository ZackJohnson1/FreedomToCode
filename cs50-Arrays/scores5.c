#include <stdio.h>

//Constants
const int N = 3;

//Prototypes
float average(int length, int array[]);

int main(void)
{
    //Get scores
    int scores(N);
    for (int i = 0; i < N; i++)
    {
        scores[i] = get_int("Score: ");
    }
}